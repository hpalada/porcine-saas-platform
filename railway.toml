[build]
builder = "NIXPACKS"
buildCommand = "cd backend && npm install && rm -rf dist && npm run build"

[deploy]
startCommand = "cd backend && node dist/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
